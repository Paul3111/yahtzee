const { useAsyncError } = require('react-router-dom');
const Game = require('../models/game');
const { ObjectId } = require("mongodb");

const GameController = {
    CreateGame: (req, res) => {
        console.log("byebyebyey");
    },

};

module.exports = GameController;